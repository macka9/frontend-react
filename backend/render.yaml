services:
  - type: web
    name: flask-backend
    env: python
    region: frankfurt
    rootDirectory: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: FLASK_ENV
        value: production
